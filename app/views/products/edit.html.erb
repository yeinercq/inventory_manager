<div class="container">
  <%= link_to sanitize("&larr; #{t('common.back')} #{t('products.singular')}"), :back %>
  <div class="card shadow mt-4">
    <div class="card-body">

      <h1><%= t('products.edit_product') %></h1>

      <%= render "form", product: @product %>
    </div>
  </div>
</div>
