<div class="container">
  <%= link_to sanitize("&larr; #{t('common.back')} coffee purchases"), :back %>
  <div class="card shadow mt-4">
    <div class="card-body">
      <h1>Editar compra de cafe</h1>
      <%= turbo_frame_tag @coffee_purchase do %>
        <%= render "form", location: @location, coffee_purchase: @coffee_purchase %>
      <% end %>
    </div>
  </div>
</div>
