<% available_events_for(sale).each do |event| %>
  <%= link_to event, [:trigger, location, sale, event: event ], data: { turbo_method: :patch, turbo_confirm: "Are you sure?" }, class: 'dropdown-item' %>
<% end %>
